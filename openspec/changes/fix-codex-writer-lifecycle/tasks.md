<!-- openspec-review-contract:v3 -->
UI contract: none

## 1. <!-- openspec-wave:codex-writer-lifecycle --> Codex writer lifecycle backport

- [x] 1.1 <!-- openspec-trace: requirements=REQ-CWL-001,REQ-CWL-002,REQ-CWL-003; verification=run the architecture planning review and apply-phase architecture validator and require Verdict READY before production edits --> <!-- openspec-review:architecture --> Pre-implementation architecture checkpoint. Coverage: baseline branch/HEAD, all planning artifacts, affected lifecycle call sites, process registry, upstream `f68abff4`, and existing tests. Growth: upstream production delta is `orchestration.py +58`, `_entry.py +20`, `app.py +15`, and `native/__init__.py +4`; failed-preload cleanup is expected below 20 lines. Ownership: native orchestration owns app-server/forwarder lifecycle, runner and pane paths invoke it, process registry owns dead-owner reconciliation. Findings: no validated High; Medium requires implementation verification to map the real runner-shutdown, pane-exit/idle-reaper, dead-owner/live-owner, and failed-preload paths rather than relying only on helper tests; Low none blocking. The reviewer's repo-local-validator High was rejected after the installed skill validator passed with `ARCHITECTURE_GATE: READY` in planning phase. Exclusions: host rollout and unrelated runner refactoring. Reviewer: `/root/writer_arch_review`, independent read-only agent. Verdict: READY after validation of the external-gate-path finding.
- [x] 1.2 <!-- openspec-trace: requirements=REQ-CWL-001,REQ-CWL-003; verification=run the upstream lifecycle teardown and process-registry test modules and assert all per-session, runner-shutdown, pane-exit, dead-owner, and live-owner scenarios pass --> Backported upstream commit `f68abff4` lifecycle teardown onto `v0.7.0`; the sole conflict preserved the branch's existing `asyncio.Task[Any]` annotation while accepting the upstream lifecycle helpers and wiring.
- [x] 1.3 <!-- openspec-trace: requirements=REQ-CWL-002,REQ-CWL-003; verification=run a focused async failed-preload regression that raises active-writer from preload and asserts event client close, app-server close, registry removal, original error propagation, and idempotent retry state --> Added cleanup around resume preload and regression coverage proving client/app-server close, registry removal, original active-writer error propagation, and a clean retry on a new endpoint.
- [x] 1.4 <!-- openspec-trace: requirements=REQ-CWL-001,REQ-CWL-002,REQ-CWL-003; verification=run the focused Codex native runner lifecycle/process-registry suite and ruff checks, then compare mypy findings against the exact base and assert no new type errors --> Verified in a Linux-filesystem WSL clone without host installation: 32 focused tests passed, Ruff check/format passed for all nine changed Python files, and differential mypy reported baseline 184/current 184/new 0. Raw mypy is not globally clean on `v0.7.0`, so the evidence is a fail-closed no-new-errors comparison rather than a false clean-baseline claim.
- [x] 1.5 <!-- openspec-review:wave --> Independent lifecycle-wave review completed by `/root/writer_full_diff_review`. Initial full-pending-diff coverage found one Medium in REQ-CWL-002 cleanup ordering; remediation moved registry removal before best-effort client/app-server close on all three pre-forwarder failure paths and added close-failure regression coverage. Targeted re-review of the finding and adjacent contract reported High/Medium/Low none and `Verdict: PASS`.

## 2. Release review

- [x] 2.1 <!-- openspec-review:final --> Independent full-diff review and targeted remediation re-review completed with High 0, Medium 0, Low 0 and Verdict: PASS. Coverage: full pending diff; Requirements: REQ-CWL-001, REQ-CWL-002, REQ-CWL-003; Exclusions: host installation, service restart, remote fork creation, commit, and push; Reviewer: /root/writer_full_diff_review.
